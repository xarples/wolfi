{
  "name": "@xarples/wolfi",
  "private": true,
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "build": "npx lerna run build --stream --scope=@xarples/wolfi-vue",
    "prepack": "npm run build",
    "publish": "npx lerna publish"
  },
  "dependencies": {
    "lerna": "^3.20.2"
  },
  "devDependencies": {
    "typescript": "^3.7.3"
  },
  "engines": {
    "node": ">=8"
  }
}
